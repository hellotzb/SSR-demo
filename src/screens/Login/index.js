import React from 'react';

const Login = () => {
  return (
    <div>
      <div>Login</div>
      <button onClick={() => alert('Login')}>click</button>
    </div>
  );
};

export default Login;
